{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bun x gh-setup-hooks",
            "timeout": 120
          },
          {
            "type": "command",
            "command": "grep -q 'GH_HOST=github.com' ~/.bashrc 2>/dev/null || sed -i '/^# If not running interactively/i # GitHub CLI: ローカルプロキシ経由でもGitHubを認識させる\\nexport GH_HOST=github.com\\n' ~/.bashrc"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git push'; then echo '🔍 git push を検出しました。変更内容を確認してください。'; fi"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/validate-gh-command.sh"
          }
        ]
      }
    ]
  }
}
